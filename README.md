# Libft-Kaan
Libft is a C library that I created as part of the 42 curriculum. The main goal of the project is to recreate some standard C library functions and create additional functions that can be useful in future projects.  The project helped me practice pointers, strings, memory allocation, file descriptors and linked lists. 
